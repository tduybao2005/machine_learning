Trong báo cáo trình bày các mục:

1. Đọc dữ liệu CSV (classification_data_numpy.csv). Chia dữ liệu train/test, trực quan hóa dữ liệu để phân tích, nhận xét về dữ liệu.

2. lựa chọn mạng NN để thực hiện bài toán classification. Nói rõ lý do lựa chọn cấu hình từng lớp.

3. Lựa chọn hàm mục tiêu để phân loại 4 class, nêu lý do chọn hàm mục tiêu. Thực hiện việc huấn luyện 10 lần, nhận xét kết quả 10 lần huấn luyện đó.

4. Lấy kết quả tốt nhất, thực hiện đánh giá bằng các chỉ số accuracy, precision, recall, F1-score cho mỗi class. Nhận xét kết quả. 

5. Vẽ ma trận confusion matrix với mô hình tốt nhất để minh họa kết quả. Nhận xét kết quả

6. Kết luận

Sinh viên nộp code + file báo cáo (PDF/Word) qua email: caovankien@iuh.edu.vn. Tiêu đè email, tên file báo cáo có dạng [HocMay-BT1]HoVaTen-MSSV.
Hạn gửi bài vào 22h ngày 05/10/2025. 
Sau hạn trên nếu chưa đủ 15 bài, sẽ nhận tiếp đến tối đa 15 bài báo cáo.
Code trùng nhau hoặc có hình ảnh giống nhau sẽ được 0 điểm x3. 

